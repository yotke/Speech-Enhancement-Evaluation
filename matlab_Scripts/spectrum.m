function [signal_fft] = spectrum(signal,fs)

%signal_fft=fft(signal);
%signal_stft =
spectrogram(signal,hamming(512),256,256,'yaxis');
%plot(abs(signal_stft));
%xlabel('frequency')
%ylabel('Time')

end

